package Mtech_Package

object if_Statement {
  def main(args: Array[String]): Unit = {
    // taking a variable
    var a: Int = 50

    if (a > 90)
    {
      // This statement will execute as a > 30
      println("Mtech Class")
    }
  }
}
